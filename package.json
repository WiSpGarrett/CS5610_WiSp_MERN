{
  "name": "wisp-photo-gallery-mern",
  "version": "1.0.0",
  "description": "Photo gallery with location mapping",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run start",
    "client": "cd frontend && npm start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
} 